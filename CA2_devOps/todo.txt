
1. Add sideBar to forms page for users to switch between models used for prediction
2. Get fileUpload working, user able to upload file and saved into directory
3. Whenever user upload file, file uploaded will be returned ( displayed in the page )

4. Add Options to configure image   ( resize, crop, etc )
5. 


env/Scripts/activate
python -m flask run --reload
pip install -r requirements.txt
python -m pytest -v
